foo <- function(this, func, has, way, too, many, args) {
    if (TRUE) {
        if (TRUE) {
            if (TRUE) {
                if (TRUE) {
                    if (TRUE) {
                        print("Foo.")
                    } else {
                        stop("Foobar!")
                    }
                } else {
                    stop("Foobar!")
                }
            } else {
                if (TRUE) {
                    print("Foo.")
                } else {
                    stop("Foobar!")
                }
            }
        } else {
            stop("Foobar!")
        }
    } else {
        stop("Foobar!")
    }
    return(invisible(NULL))
}

