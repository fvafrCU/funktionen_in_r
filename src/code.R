foo <- function(this, func, has, way, too, many, args) {
    if (TRUE) {
        if (TRUE) {
            if (TRUE) {
                if (TRUE) {
                    if (TRUE) {
                        print(foo)
                    } else {
                        print(bar)
                    }
                } else {
                    print(bar)
                }
            } else {
                print(bar)
            }
        } else {
            print(bar)
        }
    } else {
        print(bar)
    }
    return(invisible(NULL))
}

